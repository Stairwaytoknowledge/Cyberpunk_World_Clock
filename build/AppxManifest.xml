<?xml version="1.0" encoding="utf-8"?>
<!--
  MSIX manifest for Microsoft Store submission.

  Before your first build, replace the three placeholders below with the
  exact values shown on your Microsoft Partner Center app page
  (Product identity -> MSIX Identity details):

      PACKAGE_IDENTITY_NAME     e.g. 12345YourName.WorldClock
      PUBLISHER_ID              e.g. CN=ABCD1234-1234-...-...-ABCDEF012345
      PUBLISHER_DISPLAY_NAME    e.g. Your Name

  The <Version> attribute must use a 4-part number ending in .0 (the Store
  rejects anything else).  build_msix.ps1 will rewrite it with -Version.
-->
<Package
    xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
    xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
    xmlns:uap5="http://schemas.microsoft.com/appx/manifest/uap/windows10/5"
    xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities"
    IgnorableNamespaces="uap uap5 rescap">

  <Identity
      Name="PACKAGE_IDENTITY_NAME"
      Publisher="CN=PUBLISHER_ID"
      Version="1.0.0.0"
      ProcessorArchitecture="x64" />

  <Properties>
    <DisplayName>World Clock</DisplayName>
    <PublisherDisplayName>PUBLISHER_DISPLAY_NAME</PublisherDisplayName>
    <Logo>assets\store\StoreLogo.png</Logo>
  </Properties>

  <Dependencies>
    <TargetDeviceFamily
        Name="Windows.Desktop"
        MinVersion="10.0.17763.0"
        MaxVersionTested="10.0.22621.0" />
  </Dependencies>

  <Resources>
    <Resource Language="en-us" />
  </Resources>

  <Applications>
    <Application
        Id="WorldClock"
        Executable="WorldClock.exe"
        EntryPoint="Windows.FullTrustApplication">
      <uap:VisualElements
          DisplayName="World Clock"
          Description="A floating desktop widget that shows the time in four cities."
          BackgroundColor="transparent"
          Square150x150Logo="assets\store\Square150x150Logo.png"
          Square44x44Logo="assets\store\Square44x44Logo.png">
        <uap:DefaultTile
            Wide310x150Logo="assets\store\Wide310x150Logo.png"
            Square310x310Logo="assets\store\Square310x310Logo.png"
            Square71x71Logo="assets\store\Square71x71Logo.png"
            ShortName="WorldClock" />
        <uap:SplashScreen Image="assets\store\SplashScreen.png" />
      </uap:VisualElements>
      <Extensions>
        <!-- Exposes a toggle in Windows Settings > Apps > Startup so users
             can choose to launch the clock at login. Enabled="false" means
             "installed as OFF"; the user opts in.  -->
        <uap5:Extension Category="windows.startupTask">
          <uap5:StartupTask
              TaskId="WorldClockStartup"
              Enabled="false"
              DisplayName="World Clock" />
        </uap5:Extension>
      </Extensions>
    </Application>
  </Applications>

  <Capabilities>
    <!-- Required because this is a Win32 app packaged for the Store. -->
    <rescap:Capability Name="runFullTrust" />
  </Capabilities>
</Package>
